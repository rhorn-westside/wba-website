{% if env.preview %}
<div class="preview-ribbon" role="status">
  <div class="wrap preview-ribbon__inner">
    <strong>Preview copy — parents cannot see this.</strong>
    <span>Your changes are saved. To put them on the real website, use the <em>Publish</em> button.</span>
  </div>
</div>
{% endif %}
